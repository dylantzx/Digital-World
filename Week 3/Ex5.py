import math
def simpsons_rule(func, n, a, b):
    h = (b-a)/3
    f_x0= a**2
    f_n=b**2
    for j in range(n/2):
        f_x = 
        pass

    
    return f_x0, round(approx,2)

f = x**2
print(simpsons_rule(func(), 1000, 1, 3))
"""f = math.sin(x)
print(simpsons_rule(f, 1000, 0, math.pi))
f = math.exp(-x)
print(simpsons_rule(f, 1000, -1, 1))"""
